# ----------------- Settings -----------------

language: english.yml # Languages available by default: english.yml, german.yml, french.yml
xp-formula: "100 + x^1.8" # x is the current level of the player
enable-placeholder-api: true
check-updates: true

rewards:
  monster-kill: 2
  achievement: 10
  player-kill: 5

level-rewards:
  1:
    commands: # Available Placeholders: %player%
      - "give %player% diamond 1"
      - "give %player% dirt 64"
    text:
      - "&3one Dirt"
      - "&e64 Diamonds"

# --------------------------------------------

config-version: 2 # DO NOT CHANGE